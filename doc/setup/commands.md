# Useful Commands - this is a placeholder until the doc is finished

## Activate env

`.\venv\Scripts\activate`.

## Bypass win sec for a sec

`Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`.
